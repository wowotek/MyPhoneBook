language: java
sudo: false

deploy:
  provider: pages
  skip-cleanup: true
  github-token: YCgwk4oaxvO2zuGWpFl1kA  # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  on:
    branch: master
